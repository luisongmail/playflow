{
  "name": "playflow",
  "private": true,
  "version": "0.0.0",
  "description": "PlayFlow — sistema de overlays para transmisión de béisbol en vivo",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "dev": "turbo dev",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "test:e2e": "playwright test",
    "download-assets": "tsx scripts/download-assets.ts",
    "bootstrap": "bash scripts/bootstrap.sh",
    "db:backup": "docker exec playflow-db mysqldump -uroot -proot_password playflow_db > infra/playflow_backup_$(date +%Y%m%d_%H%M).sql && echo '✅ Backup guardado en infra/'",
    "db:restore": "docker exec -i playflow-db mysql -uroot -proot_password playflow_db < infra/playflow_backup_$(ls -t infra/playflow_backup_*.sql 2>/dev/null | head -1 | xargs basename) && echo '✅ Restore completado'",
    "docker:restart": "docker compose down && docker compose up -d",
    "docker:reset": "pnpm db:backup && docker compose down -v && docker compose up -d && echo '⚠️  Volumen destruido. Restaura con: pnpm db:restore'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@eslint/js": "^9.30.0",
    "@playwright/test": "^1.61.1",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "eslint": "^9.30.0",
    "globals": "^15.8.0",
    "tsx": "^4.22.4",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.35.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "lucide-react": "^1.22.0"
  }
}
